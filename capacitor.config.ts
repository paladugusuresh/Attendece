import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sapphirus.edisonlearning',
  appName: 'EdisonLearning',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
